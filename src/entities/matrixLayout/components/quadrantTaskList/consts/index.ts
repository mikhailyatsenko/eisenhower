// The wrapper holds the list and its "+N below" and takes the quadrant's
// height under its header
export const LIST_STYLES = {
  WRAPPER: 'relative z-2 min-h-0 flex-1',
  LIST: 'relative flex h-full list-none flex-col overflow-x-hidden overflow-y-auto px-1',
} as const;

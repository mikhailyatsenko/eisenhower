export const TOAST_DURATION_MS = 6000;

/** How far a finger has to drag the toast sideways to dismiss it */
export const SWIPE_DISMISS_PX = 80;

/** CSS variable with the toast's distance from the bottom of the viewport */
export const TOAST_BOTTOM_VAR = '--toast-bottom';

/** Gap between the toast and an element it stands on */
export const TOAST_CLEARANCE_GAP_PX = 8;
